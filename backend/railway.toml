[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=/app alembic upgrade head 2>&1 || true; uvicorn app.main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 30"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
